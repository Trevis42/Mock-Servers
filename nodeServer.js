/*
Use node HTTP to create a server:

it must respond to HTTP POST requests using either node HTTP module, or express module
	Response:
		1. Must respond with a random quote from a "database" of at least ten quotes.
Implement a mock of the Happy Eyes algorithm, benchmark, and report the results
*/
